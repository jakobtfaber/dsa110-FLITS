# FLITS `docs/rse/` — thin workflow residue

Manuscript / ApJ execution authority lives in the **Faber2026** parent:
`docs/rse/` (wayfinder, BOARD, verification-protocol, certificates).

This folder keeps only FLITS-local specs still cited by pipeline code or
reproducibility notes. Session handoffs and orphan skill plans were deleted
(2026-07-19 scrub). Do not resurrect them — use git history if needed.

Live ADRs: `docs/adr/`. Getting started: `docs/README.md`.
