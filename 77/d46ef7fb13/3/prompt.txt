Take over from previous session's end-state:
---
  Branches
  - figures/vector-clean (0017196) — PR-ready: my 14 verified files on origin/main, nothing else. Push
  this when you're ready (gated one-way door).
  - feat/figure-vector (3d27970) — my work + a background auto-committer's other-lane commit 7cae74e
  (crossmatching/galaxies/joint_ladder/…) beneath it. Don't push as-is.
  - lane/autocommit-otherlane (7cae74e) — that other-lane commit pinned so it's never lost.

  Gates — all clear: verify-gate (incl. this turn's joint_ppc.py crop fix), figure-review,
  deferred-tasks.

  Audit outcome — GO. Only actionable flag (save_fig's default tight-crop silently changing joint_ppc's
  PNG) neutralized with bbox_inches=None → format-only change, raster crop preserved. E402/B007 left:
  pre-existing repo-wide sys.path-shim pattern, ruff-noxfile-only, not mine to churn.

  Remaining (all gated/blocked — not closeable here):
  - Push figures/vector-clean / open PR.
  - Faber2026: fold the budget.tex block + chime_subband_compare.pdf/svg into draft/fork-b-finish, push
  to Overleaf.
  - tau_nu_ladder + joint_ppc_montage: need HPCC *_joint_samples.npz → connect Caltech VPN first.

  One watch-item: the background "carry forward" auto-committer is live and dumps onto the checked-out
  branch (feat/figure-vector). figures/vector-clean is not checked out, so it stays pristine — push from
  there.