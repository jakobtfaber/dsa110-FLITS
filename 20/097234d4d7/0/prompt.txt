You are an adversarial reviewer. Your job is to TRY TO BREAK the conclusions in a
scattering-fit analysis, not to agree with them. Be skeptical and quantitative.

Context: joint CHIME(400-800MHz)+DSA(1.2-1.5GHz) scattering fits. Each FRB ("burst")
was fit with a shared scattering index alpha and tau_1ghz. Models live in JSON at:
  analysis/scattering-refit-2026-06/joint_ladder/*_joint_fit*.json

File naming:
  <burst>_joint_fit.json            = "base" joint fit (single comp, free per-band zeta, ML-profiled gain)
  <burst>_joint_fit_sharedzeta.json = shared-zeta single-comp joint fit
  <burst>_joint_fit_CxDy.json       = x CHIME + y DSA temporal components, profiled gain variance s2
  <burst>_joint_fit_CxDy_s2-N.json  = same but with gain-prior variance s2 FIXED to N (N in {1,10,100})

Each JSON has: alpha.{median,err_plus,err_minus}, tau_1ghz.{...}, log_evidence,
log_evidence_err, alpha_bounds (=[1.5,6.0] for component runs), components_C/D, gain_s2.

The analysis (in analysis/scattering-refit-2026-06/joint_ladder/LADDER_SUMMARY.md) claims:

CLAIM 1 — Evidence comparability: profiled-s2 log_evidence is empirical-Bayes (a
profile Z) and is NOT comparable across component count N; the base/sharedzeta rows
sit on a different likelihood normalization (~-1e4) than CxDy runs (~+2e4), so you
must not subtract lnZ across those families. Only the FIXED-s2 ladder gives a clean
cross-N Bayes factor.

CLAIM 2 — Component selection via fixed-s2 sign-flip: a component is real iff
ΔlnZ(N+1 vs N) is consistently positive (>~5) across s2 in {1,10,100}; a sign flip
with prior scale ⇒ prior-driven, not real. Reported:
  whitney   C2D1 vs C1D1: -5.3/+1.7/-4.6  -> 2nd comp NOT real (single)
  johndoeII C2D2 vs C2D1: +150.9/-126.3/+119.7 -> 2nd DSA comp NOT real
  phineas   C3D1 vs C2D1: +1.7/+6.2/-1.2 -> 3rd CHIME comp not robust at D1

CLAIM 3 — alpha gate: physical bound 1.5<alpha<6.0; alpha AT a prior edge ⇒ FAIL
(railed posterior is not a measurement).

CLAIM 4 — per-burst railing verdicts:
  hamilton  = complete FAIL (base alpha=1.01 below floor; sharedzeta & C1D1 rail at 1.50; no usable alpha)
  whitney   = RECOVERABLE: base joint alpha=3.75+-0.04 is clean/in-bounds; only shared-zeta/component path rails at 1.52
  johndoeII = genuinely low/unstable: every parameterization low (base 1.06, railed 1.50, best off-rail C2D2 1.95); no clean number
  8 PASS: freya4.36 mahi3.80(single) phineas3.33 chromatica3.29 oran2.69(2comp) wilhelm2.56 zach2.41 casey2.40
  isha = MARGINAL 5.39 +0.50/-1.95

YOUR TASK — read the actual JSONs and check each claim. Specifically attack:
 (a) Are the fixed-s2 deltas computed correctly and do they really sign-flip? Recompute from the raw JSONs.
 (b) Is the railing diagnosis sound? An alpha median within ~1 sigma of the bound with a one-sided error bar is the railing signature — verify per burst (hamilton/whitney/johndoeII) using alpha.median, err_minus, and alpha_bounds[0].
 (c) Is CLAIM 1 defensible? Is there any case where the summary's verdict secretly depends on comparing non-comparable lnZ (e.g. picking a "best model" per burst by mixing base/sharedzeta with CxDy)? Check the PASS table: casey/chromatica/freya/wilhelm have only base+sharedzeta — is choosing sharedzeta there legitimate (same family, comparable)?
 (d) whitney "recoverable" hinges on the base joint alpha=3.75 being trustworthy while the component fit rails. Is that internally consistent or special pleading? Note base whitney tau=1.09ms vs component tau=0.49ms — does that disagreement undermine BOTH?
 (e) Any burst where the PASS verdict is overstated (e.g. isha alpha=5.39 with err_minus=1.95 — is calling it MARGINAL generous or too harsh? is oran's 2nd-component claim from a profiled Δ only, with no fixed-s2 confirmation?)

Output a concise verdict: for each claim CONFIRM / REFUTE / PARTIAL with the specific
numbers you recomputed. Flag any conclusion you can actually break. Do not rewrite files.